<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Panel de Administración</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f5f7;
        }
        .sidebar {
            height: 100vh;
            background-color: #6a1b9a;
            color: white;
            padding: 1rem;
        }
        .sidebar a {
            color: white;
            display: block;
            margin: 1rem 0;
            text-decoration: none;
        }
        .sidebar a:hover {
            text-decoration: underline;
        }
        .card {
            border: none;
            border-radius: 1rem;
        }
    </style>
</head>
<body>

<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <div class="col-md-2 sidebar">
            <h4 class="mb-4">🌿 Webscraping Admin</h4>
            <div class="mb-3">
                <img src="https://via.placeholder.com/60" alt="Admin" class="rounded-circle">
                <p class="mt-2">Admin</p>
            </div>
            <a href="/admin/dashboard"><i class="bi bi-house-door-fill"></i> Dashboard</a>
            <a href="/admin/plantas"><i class="bi bi-flower1"></i> Plantas</a>
            <a href="/admin/categorias"><i class="bi bi-tags-fill"></i> Categorías</a>
            <a href="/"><i class="bi bi-globe"></i> Volver al sitio</a>
            <form action="/admin/logout" method="post" class="mt-4">
                <button class="btn btn-danger btn-sm w-100">Cerrar Sesión</button>
            </form>
        </div>

        <!-- Main Content -->
        <div class="col-md-10 p-4">
            <h2>Dashboard</h2>

            <!-- Cards -->
            <div class="row mt-4">
                <div class="col-md-4 mb-4">
                    <div class="card text-white bg-danger p-4 shadow">
                        <div>
                            <h5>Ventas semanales</h5>
                            <h3>$15,000</h3>
                            <small>⬆️ Aumento del 60%</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="card text-white bg-primary p-4 shadow">
                        <div>
                            <h5>Pedidos semanales</h5>
                            <h3>45,633</h3>
                            <small>⬇️ Disminuyó 10%</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="card text-white bg-success p-4 shadow">
                        <div>
                            <h5>Visitantes online</h5>
                            <h3>95,741</h3>
                            <small>⬆️ Aumento del 5%</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Placeholder para gráfico -->
            <div class="card mt-4 p-4 shadow">
                <h5>Visitantes y estadísticas de ventas</h5>
                <p>Aquí irá un gráfico próximamente.</p>
            </div>
        </div>
    </div>
</div>

</body>
</html>

