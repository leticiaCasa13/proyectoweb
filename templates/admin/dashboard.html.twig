<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Panel de Administración</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
    body {
        background-color: #f4f5f7;
    }

    .sidebar {
        height: 100vh;
        background-color: #6a1b9a;
        color: white;
        padding: 1rem;
    }

    .sidebar a {
        color: white;
        display: block;
        margin: 1rem 0;
        text-decoration: none;
    }

    .sidebar a:hover {
        text-decoration: underline;
    }

    .card {
        border: none;
        border-radius: 1rem;
    }

    /* Tarjetas personalizadas */
    .card-verde-medio {
        background-color: #388e3c; /* Verde medio */
        color: white;
    }

    .card-verde-claro {
        background-color: #81c784; /* Verde clarito */
        color: white;
    }

    .card-gris {
        background-color: #9e9e9e; /* Gris */
        color: white;
    }


    .sidebar {
    background-color: #d0f0c0; /* Verde clarito */
    color: #333;               /* Texto oscuro para contraste */
    border-right: 1px solid #ccc; /* Borde gris a la derecha */
    min-height: 100vh;
    padding: 1rem;
   }
  .sidebar a {
    display: block;
    margin-bottom: 1rem;
    color: #333;
    text-decoration: none;
  }
  .sidebar a:hover {
    color: #2e7d32; /* Verde más oscuro al pasar el ratón */
 }

 .btn-verde-claro {
    background-color: #d0f0c0; /* Verde clarito */
    color: #333;
    border: 1px solid #ccc;
 }
 .btn-verde-claro:hover {
    background-color: #c0e6b0; /* Un poco más oscuro al pasar el mouse */
    color: #222;
 }


</style>

</head>
<body>

<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <div class="col-md-2 sidebar">
            <h4 class="mb-4">Webscraping Admin</h4>
            <div class="mb-5">
             <img src="/assets/images/logAdmin.jpg" alt="Admin" class="rounded-circle">
               <p class="mt-4">Admin</p>

            </div>
            <a href="/admin/dashboard"><i class="bi bi-house-door-fill"></i> Dashboard</a>
            <a href="/admin/plantas"><i class="bi bi-flower1"></i> Plantas</a>
            <a href="/admin/categorias"><i class="bi bi-tags-fill"></i> Categorías</a>
            <a href="/"><i class="bi bi-globe"></i> Volver al sitio</a>
            <form action="/admin/logout" method="post" class="mt-4">
                <button class="btn btn-verde-claro btn-sm w-100">Cerrar Sesión</button>

            </form>
        </div>

        <!-- Main Content -->
        <div class="col-md-10 p-4">
            <h2>Dashboard</h2>

            <!-- Cards -->
            <div class="row mt-4">
    <div class="col-md-4 mb-4">
        <div class="card card-verde-medio p-4 shadow">
            <div>
                <h5>Ventas semanales</h5>
                <h3>$15,000</h3>
                <small>⬆️ Aumento del 60%</small>
            </div>
        </div>
    </div>
    <div class="col-md-4 mb-4">
        <div class="card card-verde-medio p-4 shadow">
            <div>
                <h5>Pedidos semanales</h5>
                <h3>45,633</h3>
                <small>⬇️ Disminuyó 10%</small>
            </div>
        </div>
    </div>
    <div class="col-md-4 mb-4">
        <div class="card card-verde-medio p-4 shadow">
            <div>
                <h5>Visitantes online</h5>
                <h3>95,741</h3>
                <small>⬆️ Aumento del 5%</small>
            </div>
        </div>
    </div>
</div>



            <!-- Placeholder para gráfico -->
            <div class="card mt-4 p-4 shadow">
                <h5>Distribución por Categoría de Plantas</h5>
                <p>Aquí irá un gráfico próximamente.</p>
                <canvas id="graficoCategorias" width="400" height="400"></canvas>
            </div>
        </div>
    </div>
</div>
    
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <script>
        const ctx = document.getElementById('graficoCategorias').getContext('2d');

         const categorias = {{ categorias|json_encode()|raw }};
         const cantidades = {{ cantidades|json_encode()|raw }};


        const colores = [
            '#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0', '#9966FF', '#FF9F40'
        ];

        new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: categorias,
                datasets: [{
                    data: cantidades,
                    backgroundColor: colores,
                    borderWidth: 1
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: {
                        position: 'bottom',
                    },
                    title: {
                        display: true,
                        text: 'Cantidad de Plantas por Categoría'
                    }
                }
            }
        });
    </script>
</body>
</html>



