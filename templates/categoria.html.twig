<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ categoria.nombre }}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet">

    
    <!-- Estilos personalizados -->
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    {% include 'layouts/header.html.twig' %} {# Incluye el menú principal #}

 <!-- CONTENIDO -->
<main class="container my-5">
    <div class="categoria-titulo mb-4 text-center text-md-start">
        <h1>{{ categoria.nombre }}</h1>
        <p class="text-muted">{{ planta.descripcion }}</p>
    </div>

    <div class="row">
        {% for planta in plantas %}
        <div class="col-sm-6 col-md-4 col-lg-3 mb-4">
            <div class="card h-100">
                <a href="/planta/{{ planta.id }}" title="{{ planta.nombre }}">
                  <img src="{{ planta.imagen_url }}" class="card-img-top" alt="{{ planta.nombre }}" 
                  style="height: 250px; object-fit: cover;">
                </a>

                <div class="card-body d-flex flex-column">
                    
                    <h5 class="categoria-titulo mb-4 text-center text-md-start text-muted">{{ planta.nombre }}</h5>
                    <p class="card-text text-muted">{{ planta.descripcion }}</p>
                    <p class="card-text mt-auto text-success"><strong>Precio:</strong> ${{ planta.precio }}</p>
                    
   
                    
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</main>


    {% include 'layouts/footer.html.twig' %} {# Incluye el pie de página #}
</body>
</html>


<h5 class="card-title text-success">{{ planta.nombre }}</h5>
<p class="card-text text-muted">{{ planta.descripcion }}</p>
<p class="card-text mt-auto text-danger"><strong>Precio:</strong> ${{ planta.precio }}</p>

